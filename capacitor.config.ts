import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glennpimentel.saltandlight',
  appName: 'Salt & Light',
  webDir: 'dist'
};

export default config;
